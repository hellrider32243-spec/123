# Вставить в HTTP server :80 (wingsvpn-origin.conf), до остальных location.
# Нужен для Let's Encrypt HTTP-01 (Hysteria2 TLS).
location ^~ /.well-known/acme-challenge/ {
    root /opt/nordwings/web;
    default_type text/plain;
}
